# =============================================================================
# worker/requirements.txt — System Windykacja ARQ Worker
# =============================================================================

# ── ARQ (Async task queue) ───────────────────────────────────────────────────
arq==0.26.1
redis[hiredis]==5.2.1

# ── FastAPI (Worker REST API port 8001) ──────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Database (MSSQL async) ───────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
aioodbc==0.5.0
pyodbc==5.2.0

# ── Settings & validation ────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Email ────────────────────────────────────────────────────────────────────
aiosmtplib==3.0.2

# ── PDF (WeasyPrint HTML→PDF) ────────────────────────────────────────────────
weasyprint==60.2
reportlab==4.2.5
jinja2==3.1.4

# ── HTTP client (SMS gateway) ────────────────────────────────────────────────
httpx==0.28.1

# ── HTTP client synchroniczny — TYMCZASOWA NAPRAWA 2026-07-17 ────────────────
# worker/services/ksef20_adapter.py uzywa requests.Session() (nie httpx) —
# fragment przeniesiony z zewnetrznego pliku referencyjnego (projekt
# SAMARYTANIN, worker_ksef20_export.py) bez przepisania na httpx zgodnie
# z konwencja reszty projektu. Brakujaca zaleznosc powodowala
# ModuleNotFoundError przy kazdej synchronizacji zrodel source_type='ksef20'.
# SWIADOMA DECYZJA: nie przepisywac na httpx w tym momencie — kod dotyczy
# kryptografii i protokolu autoryzacji z systemem KSeF (XAdES, AES/RSA),
# nie da sie dzis zweryfikowac integracyjnie bez certyfikatu testowego MF.
# Docelowo do przepisania na httpx.Client(), gdy dostepny bedzie certyfikat
# testowy pozwalajacy zweryfikowac zachowanie na zywo.
requests==2.32.3

# ── Serialization ────────────────────────────────────────────────────────────
orjson==3.10.12

# ── Kryptografia (Fernet — deszyfrowanie connection_config zrodel) ───────────
# Wersja zweryfikowana zgodnie z tym, co realnie zainstalowane w windykacja_api
# (docker exec windykacja_api pip show cryptography → 49.0.0, 2026-07-10)
cryptography==49.0.0
signxml==5.1.0
lxml==6.1.1

paramiko==5.0.0

# ── Date/Time (timezone support) ─────────────────────────────────────────────
python-dateutil==2.9.0

# ── Compression (snapshots) ──────────────────────────────────────────────────
# stdlib gzip — no extra package needed

# ── Monitoring ───────────────────────────────────────────────────────────────
psutil==6.1.0


# ── OCR (F7) ──────────────────────────────────────────────────────────────────
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0